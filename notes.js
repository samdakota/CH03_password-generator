// stringify array options
let array = [];
let stringOne = array.toString();

let stringTwo = JSON.stringify(array);

// without commas
[].join("");



Math.floor(Math.random() * array.length)

for (i = 0; i <= length; i++)